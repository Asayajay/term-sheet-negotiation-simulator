# Postgres driver, kept separate from requirements.txt so local dev/test
# (which defaults to SQLite) doesn't need a pg_config toolchain available.
# Installed in the Docker image, where DATABASE_URL points at Postgres.
psycopg2-binary==2.9.9
